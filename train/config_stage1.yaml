# Model arguments
# Local asset path: training nodes have no public internet (HF hub is reached
# only via hf-mirror through the whitelist proxy), and the parity runs must
# load byte-identical weights on every run.
model_name_or_path: /user/dengxianglong/models/Qwen3-0.6B
model_revision: main
torch_dtype: bfloat16
attn_implementation: flash_attention_2

# Data training arguments
chat_template: "{%- if tools %}\n    {{- '<|im_start|>system\\n' }}\n    {%- if messages[0].role == 'system' %}\n        {{- messages[0].content + '\\n\\n' }}\n    {%- endif %}\n    {{- \"# Tools\\n\\nYou may call one or more functions to assist with the user query.\\n\\nYou are provided with function signatures within <tools></tools> XML tags:\\n<tools>\" }}\n    {%- for tool in tools %}\n        {{- \"\\n\" }}\n        {{- tool | tojson }}\n    {%- endfor %}\n    {{- \"\\n</tools>\\n\\nFor each function call, return a json object with function name and arguments within <tool_call></tool_call> XML tags:\\n<tool_call>\\n{\\\"name\\\": <function-name>, \\\"arguments\\\": <args-json-object>}\\n</tool_call><|im_end|>\\n\" }}\n{%- else %}\n    {%- if messages[0].role == 'system' %}\n        {{- '<|im_start|>system\\n' + messages[0].content + '<|im_end|>\\n' }}\n    {%- endif %}\n{%- endif %}\n{%- set ns = namespace(multi_step_tool=true, last_query_index=messages|length - 1) %}\n{%- for message in messages[::-1] %}\n    {%- set index = (messages|length - 1) - loop.index0 %}\n    {%- if ns.multi_step_tool and message.role == \"user\" and message.content is string and not(message.content.startswith('<tool_response>') and message.content.endswith('</tool_response>')) %}\n        {%- set ns.multi_step_tool = false %}\n        {%- set ns.last_query_index = index %}\n    {%- endif %}\n{%- endfor %}\n{%- for message in messages %}\n    {%- if message.content is string %}\n        {%- set content = message.content %}\n    {%- else %}\n        {%- set content = '' %}\n    {%- endif %}\n    {%- if (message.role == \"user\") or (message.role == \"system\" and not loop.first) %}\n        {{- '<|im_start|>' + message.role + '\\n' + content + '<|im_end|>' + '\\n' }}\n    {%- elif message.role == \"assistant\" %}\n        {%- set reasoning_content = '' %}\n        {%- if message.reasoning_content is string %}\n            {%- set reasoning_content = message.reasoning_content %}\n        {%- else %}\n            {%- if '</think>' in content %}\n                {%- set reasoning_content = content.split('</think>')[0].rstrip('\\n').split('<think>')[-1].lstrip('\\n') %}\n                {%- set content = content.split('</think>')[-1].lstrip('\\n') %}\n            {%- endif %}\n        {%- endif %}\n        {%- if loop.index0 > ns.last_query_index %}\n            {%- if loop.last or (not loop.last and reasoning_content) %}\n                {{- '<|im_start|>' + message.role + '\\n<think>\\n' + reasoning_content.strip('\\n') + '\\n</think>\\n\\n' + content.lstrip('\\n') }}\n            {%- else %}\n                {{- '<|im_start|>' + message.role + '\\n' + content }}\n            {%- endif %}\n        {%- else %}\n            {{- '<|im_start|>' + message.role + '\\n' + content }}\n        {%- endif %}\n        {%- if message.tool_calls %}\n            {%- for tool_call in message.tool_calls %}\n                {%- if (loop.first and content) or (not loop.first) %}\n                    {{- '\\n' }}\n                {%- endif %}\n                {%- if tool_call.function %}\n                    {%- set tool_call = tool_call.function %}\n                {%- endif %}\n                {{- '<tool_call>\\n{\"name\": \"' }}\n                {{- tool_call.name }}\n                {{- '\", \"arguments\": ' }}\n                {%- if tool_call.arguments is string %}\n                    {{- tool_call.arguments }}\n                {%- else %}\n                    {{- tool_call.arguments | tojson }}\n                {%- endif %}\n                {{- '}\\n</tool_call>' }}\n            {%- endfor %}\n        {%- endif %}\n        {{- '<|im_end|>\\n' }}\n    {%- elif message.role == \"tool\" %}\n        {%- if loop.first or (messages[loop.index0 - 1].role != \"tool\") %}\n            {{- '<|im_start|>user' }}\n        {%- endif %}\n        {{- '\\n<tool_response>\\n' }}\n        {{- content }}\n        {{- '\\n</tool_response>' }}\n        {%- if loop.last or (messages[loop.index0 + 1].role != \"tool\") %}\n            {{- '<|im_end|>\\n' }}\n        {%- endif %}\n    {%- endif %}\n{%- endfor %}\n{%- if add_generation_prompt %}\n    {{- '<|im_start|>assistant\\n' }}\n    {%- if enable_thinking is defined and enable_thinking is false %}\n        {{- '<think>\\n\\n</think>\\n\\n' }}\n    {%- endif %}\n{%- endif %}"
dataset_name: /user/dengxianglong/datasets/ReasonLite-Dataset # stage1 data
dataset_config: default
# open-r1 sft.py reads `dataset_train_split` (TRL ScriptArguments), NOT
# `dataset_split` — the latter is silently ignored. The published dataset on
# this cluster exposes only `high` (92 shards) and `medium` (217 shards); the
# recipe's original `cot` split does not exist here. stage1 trains on `medium`.
dataset_train_split: medium
# Tokenize renders the Jinja chat template above per row, in Python, across
# 4.33M long-CoT rows. It is the dominant pre-training cost.
# 96 matches the 105 cores the runs allocate per node, leaving headroom for the
# main process, dataloader workers and the system.
# Note on the measurements: orig_1_2k took 1:24:55 at 850 examples/s, and a
# single-rank smoke at num_proc=96 took 7:52 at 9,165 -- but those two numbers
# are not comparable. The 10.8x gap is mostly NOT this setting (workers only
# rose 1.5x); it is that random tokenize fingerprints made all 16 ranks tokenize
# the full split independently, ~1024 processes on 105 cores. The cache-key fix
# removes that contention; this value only right-sizes one rank's worker pool.
# CAUTION: this value is part of the shard cache filename
# (cache-<fp>_00000_of_00096.arrow). Changing it invalidates the tokenize cache
# and forces a full re-tokenize; do not tune it casually.
dataset_num_proc: 96
ddp_timeout: 65536
eos_token: <|im_end|>

# SFT trainer config
# Loss normalisation denominator: global token count across ranks (True) or the
# local rank's (False). It enters backprop, so it changes the gradients, not just
# the reported number.
#
# Set explicitly because the default moved underneath us: upstream flipped it
# from False to True between 4.52 and 4.56, and neither side of the parity
# experiment set it, so the baseline (4.56) silently ran True and the packed side
# (4.52) False. That made it the one environment difference specific enough to
# test with a single variable -- which is what this run does, with both sides
# pinned to True. Pinning also stops the next version bump from moving the
# training maths with nothing in this repo changing.
average_tokens_across_devices: true
bf16: true
do_eval: false
eval_strategy: 'no'
gradient_accumulation_steps: 1
gradient_checkpointing: true
gradient_checkpointing_kwargs:
  use_reentrant: false
hub_model_id: ReasonLite_stage1
hub_strategy: every_save
learning_rate: 3.0e-04
log_level: info
logging_steps: 1
logging_strategy: steps
lr_scheduler_type: cosine_with_min_lr
lr_scheduler_kwargs:
  min_lr_rate: 0.1
packing: false
max_grad_norm: 0.3
max_length: 32768
# Parity experiment: 2000 optimizer steps, not the full 8-epoch recipe. The full
# recipe is ~60h per run on 32 H100s; three runs do not fit the shared quota.
# All parity runs use the SAME max_steps so the warmup_ratio=0.03 LR curve
# (60 warmup steps + 1940 steady) is identical across them.
#
# Raised from the first pass's 300 because the packed run's deviation was
# already separating from the baseline noise floor there -- the ratio grew from
# 2.8x over steps 1-50 to 14-26x over steps 151-300 -- and the open question is
# whether that separation keeps widening. Resuming the 300-step checkpoints was
# rejected: warmup_ratio and the cosine decay both scale with total steps, so
# resuming at max_steps=2000 would jump the LR ~10x at the seam.
# Normalisation experiment: run real stage1's LR curve, not a step-capped one.
# max_steps: -1 lets the trainer derive the horizon from the data exactly as the
# upstream recipe does -- ceil(4_333_874 / 256) = 16_930 steps/epoch x 8 epochs
# = 135_440 steps, matching what train_opt's completed full run reported. warmup
# then follows from warmup_ratio: ceil(135_440 * 0.03) = 4_064 steps.
#
# This replaces the 2000-step cap, whose warmup_ratio produced only 60 warmup
# steps -- a 68x shorter LR ramp than real training ever sees, which made the
# early-training regime the parity runs measured unrepresentative of it.
#
# The run is stopped manually once both arms pass 10_000 steps. Stopping early
# does not perturb the curve; shortening max_steps would, because both warmup
# and the cosine decay scale with the horizon.
max_steps: -1
num_train_epochs: 8
# Placeholder only. Every parity run overrides --output_dir with a run-unique
# timestamped path so no run can resume from or overwrite another's checkpoints
# (the repro tree holds a real checkpoint-16600 that auto-resume would pick up).
output_dir: /user/dengxianglong/outputs/parity/stage1_baseline
overwrite_output_dir: false
per_device_eval_batch_size: 1
# 16 GPUs (2 nodes x 8) x per_dev 16 x grad_accum 1 = global batch 256, the
# recipe's value. per_dev=32 on 8 GPUs OOMs at max_length=32768 (backward
# activations exceed 80G); this 16-GPU shape is the one the throughput branch
# already validated on this cluster.
per_device_train_batch_size: 16
push_to_hub: false
save_steps: 100
save_total_limit: 30
seed: 42
use_liger_kernel: true
warmup_ratio: 0.03
