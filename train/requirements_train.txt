# Training dependencies for ReasonLite on H100 (open-r1 SFT stack).
# Installed at runtime by launch_h100.sh on top of the infra/nvidia-pytorch
# base image.
#
# torch, flash-attn, and the CUDA stack are PROVIDED BY THE BASE IMAGE
# (verified: python 3.12.3, torch 2.7.0a0+nv25.04, flash_attn 2.7.3).
# Do NOT list them here: pip would downgrade torch (e.g. to 2.6.0) and break
# the CUDA / flash-attn bindings compiled into the image wheels.
#
# dill 0.3.9 and pyarrow 19.0.1 are also preinstalled in the image. datasets
# 3.x pins dill<0.3.9 (incompatible) and datasets 4.0+ pins pyarrow>=21.0.0,
# so we pin datasets==4.0.0 and explicitly upgrade pyarrow to >=21.0.0
# (compatible with the image's dill 0.3.9, which datasets 4.x does not bound).
transformers==4.56.0
accelerate==1.4.0
trl==0.18.0
datasets==4.0.0
pyarrow>=21.0.0
deepspeed==0.16.8
peft>=0.14.0
liger-kernel>=0.5.10
safetensors>=0.3.3
sentencepiece>=0.1.99
einops>=0.8.0
huggingface-hub[cli,hf_xet]>=0.30.2,<1.0
hf_transfer>=0.1.4
math-verify==0.5.2
latex2sympy2_extended>=1.0.6
langdetect
bitsandbytes>=0.43.0
wandb>=0.19.1
